[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter api build"

[deploy]
startCommand = "pnpm --filter api run migration:run && pnpm --filter api run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
